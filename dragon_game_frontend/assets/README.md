# Assets Folder

Place all static assets and 3D models for the Dragon Game Frontend here.

- `toothless.glb` &mdash; 3D dragon model for import into the game (Required).
- Any additional images, textures, or future assets.

Assets should be referenced via `process.env.PUBLIC_URL + "/assets/[filename]"` in code.
